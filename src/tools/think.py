"""Scratchpad think tool — gives Claude a structured slot to reason before acting."""


def handle_think(thought: str) -> str:
    """Use this to reason step-by-step before acting. Output is not shown to the user."""
    return thought
