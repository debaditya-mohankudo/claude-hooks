{
  "concepts": {
    "cache-store": {
      "confidence": 0.8,
      "contracts": [
        "get_cache(name) returns (and lazily creates) the dict for that cache name; callers key entries within it by path/id themselves.",
        "list_caches() enumerates registered cache names for introspection.",
        "hooks/server.py exposes GET /cache (names -> keys, content-free) and GET /cache/{name} (full content) over HTTP for any registered cache without needing a new endpoint per cache."
      ],
      "created_at": "2026-08-20T23:23:38.029699+00:00",
      "description": "In-memory, name-keyed registry of dict-of-dicts caches shared across the long-running hooks FastAPI server process. Introduced to survive transient iCloud file-provider read locks (EDEADLK) when reading LIFE_OS vault files in dispatcher.py's _load_vault_context().",
      "evidence": [
        "hooks/cache_store.py:get_cache",
        "hooks/dispatcher.py:_load_vault_context",
        "hooks/server.py"
      ],
      "invariants": [
        "Cache lives in process memory only — no disk persistence; a server restart clears it. Acceptable because dispatcher.py is imported once into the long-running server process, not the short-lived CLI fallback path.",
        "A cache read failure (e.g. transient OSError) should fall back to the last successfully cached value rather than propagate, logged at INFO/DEBUG once a cached value exists (WARNING only on a first-ever failed read with nothing cached yet)."
      ],
      "last_validated": "2026-08-20T23:23:38.030347+00:00",
      "module": "hooks/cache_store.py",
      "name": "cache-store"
    },
    "concept-store-symbol-only-evidence": {
      "confidence": 0.8,
      "contracts": [
        "ConceptStore.upsert()/get()/list() are unaware of the evidence format — validation lives only in the test asserting the citation regex, not in store.py itself"
      ],
      "created_at": "2026-08-17T17:17:54.663352+00:00",
      "description": "ConceptStore's evidence field cites code locations as \"file:symbol\" (or \"file:Class.method\", or bare \"file\" when nothing encloses the cited spot) — never a line number. symbol_resolver.py resolves a citation to its current line range on demand via AST, so citations survive line drift instead of going stale as code shifts around them. The extract-concepts skill (~/.claude/skills/extract-concepts/skill.md) is the sole path that writes new concepts now; the old LLM one-shot extractor.py/claude_cli.py/diff.py/diff_hook.py pipeline (which produced file:line citations and re-extracted on every Edit/Write via a PostToolUse hook) was retired.",
      "evidence": [
        "concept_store/store.py:ConceptStore",
        "concept_store/symbol_resolver.py:resolve_symbol",
        "tests/test_symbol_resolver.py"
      ],
      "invariants": [
        "every evidence string in concepts.json matches ^[^\\s:]+(:[A-Za-z_][A-Za-z0-9_]*(\\.[A-Za-z_][A-Za-z0-9_]*)?)?$ — file, file:symbol, or file:Class.method, never a line number or range",
        "resolve_symbol() only descends one level into a class (Class.method), matching how citations are ever written"
      ],
      "last_validated": "2026-08-17T17:17:54.663401+00:00",
      "module": "concept_store/store.py",
      "name": "concept-store-symbol-only-evidence"
    },
    "config-db-paths-and-domains": {
      "confidence": 0.95,
      "contracts": [
        "config.memory_db, config.tool_hints_db, config.log_db are valid Paths"
      ],
      "created_at": "2026-06-29T09:47:25.287104+00:00",
      "description": "Central configuration via pydantic-settings. Defines all database paths and memory scoring tunables. All other modules import from config singleton.",
      "evidence": [
        "src/config.py",
        "scripts/drop_memory_domain_column.py",
        "scripts/drop_tool_hints_domain_column.py"
      ],
      "invariants": [
        "MEMORY.sqlite's memories table has no domain column as of task:4c9c21e6 (2026-08-07) — domain is not a retrieval axis for score_memories() anymore, which scores tags-only across a single global batch (see memory-scoring-global-batch-limit).",
        "tool_hints.sqlite's mcp_tool_hints table has no domain column as of task:5b15dc9b (2026-08-07) — KeywordOverlapScorer (langchain_learning/retrievers.py) scores purely on keyword overlap + usage count now. cwd_domain_map, _load_cwd_domain_map, cwd_domains_json, _CWD_DOMAIN_MAP_DEFAULT were deleted from src/config.py in the same task, and cwd_domains.json (icloud_db_dir) was deleted — nothing in this repo maps CWD to a domain string anymore."
      ],
      "last_validated": "2026-08-07T14:50:43.734620+00:00",
      "module": "src/config.py",
      "name": "config-db-paths-and-domains"
    },
    "db-schema-ddl-and-migrations": {
      "confidence": 0.95,
      "contracts": [],
      "created_at": "2026-06-29T09:47:25.287416+00:00",
      "description": "Central DDL constants and additive migration functions for MEMORY.sqlite, tool_hints.sqlite, and claude_hooks.sqlite. Tests import DDL from here — no inline DDL in test files. proj_tasks.db's tables (open_tasks, task_events, commit_task_map, task_edges) and migrate_tasks_db were removed (task:87ec7876) along with src/tools/tasks.py, the actual production path that created them — this module's docstring always said those constants existed only for tests and one-time install.",
      "evidence": [
        "src/db/schema.py"
      ],
      "invariants": [
        "ADDITIVE-ONLY MIGRATION enforced structurally: migrate() can only CREATE TABLE IF NOT EXISTS or ALTER TABLE ADD COLUMN",
        "This module is not the production schema/migration path for the DBs it still covers"
      ],
      "last_validated": "2026-08-01T05:48:37.366730+00:00",
      "module": "src/db/schema.py",
      "name": "db-schema-ddl-and-migrations"
    },
    "dispatcher-hook-event-routing": {
      "confidence": 0.97,
      "contracts": [
        "Reads hook payload from stdin, writes JSON to stdout",
        "Returns additionalSystemPrompt for UserPromptSubmit",
        "Returns permissionDecision=deny for blocked PreToolUse",
        "PreToolUse handlers may also return permissionDecision=allow with additionalContext to inject a non-blocking reminder into Claude's context without denying the tool call",
        "PostToolUse handlers may also return a bare hookSpecificOutput dict (hookEventName=PostToolUse, additionalContext=...) to inject a non-blocking reminder without any permission-decision field — PostToolUse has no allow/deny concept, unlike PreToolUse"
      ],
      "created_at": "2026-06-29T09:47:25.284287+00:00",
      "description": "Single entry point for all Claude Code hook events. Routes each event type (UserPromptSubmit, PreToolUse, PostToolUse, Stop, SessionStart, SessionEnd) to its dedicated handler via a dispatch table.",
      "evidence": [
        "hooks/dispatcher.py:_HANDLERS",
        "hooks/dispatcher.py:main",
        "hooks/dispatcher.py:_handle_pre_tool_use",
        "hooks/dispatcher.py:_maybe_context_size_nudge",
        "tests/test_dispatcher.py"
      ],
      "invariants": [
        "PostToolUse returns None for non-MCP tools and for memory__ tools before reaching run_post_tool — those two early-returns exist only to skip the tool-hint/memory-scoring pipeline, not to carry any nudge. The taskfw-drift subprocess that once rode through here (_maybe_taskfw_drift_nudge / _merge_drift_nudge) was removed with task-framework's own drift nudge (task:00d9483f); do not re-add a cross-repo subprocess call on this path."
      ],
      "last_validated": "2026-08-17T17:19:36.385682+00:00",
      "module": "hooks/dispatcher.py",
      "name": "dispatcher-hook-event-routing"
    },
    "dispatcher-system-prompt-assembly": {
      "confidence": 0.95,
      "contracts": [
        "Returns a string (may be empty) to be injected as additionalSystemPrompt",
        "All sections are markdown-formatted"
      ],
      "created_at": "2026-06-29T09:47:25.284960+00:00",
      "description": "Assembles the UserPromptSubmit additionalSystemPrompt from SessionState context into markdown sections. STALE (task:c2e36050, 2026-08-14): most of this entry's contracts/invariants below describe the pre-task:882d67fa pull-based task-context pipeline (LoadRelatedTasksNode, task_body, execution_contract, activate_task.py, mid_task_decisions) — all removed, none currently exist. Needs a full re-read and rewrite, not attempted here; only the active-task section below is current.",
      "evidence": [
        "hooks/dispatcher.py:_format_system_prompt"
      ],
      "invariants": [
        "Sections are only emitted when their data is non-empty",
        "Related tasks section is populated when LoadRelatedTasksNode finds semantically similar done tasks via TurboVec neighbor search (capped at 3); empty when no active task, no rag index, or no matching done neighbors",
        "Task context summary takes precedence over raw task history when both exist",
        "New-project note is emitted when ctx['cwd_unmapped'] is true (set by CwdDomainDetectNode only on the first turn a session's cwd is unmapped, tracked via cwd_domain_reminder_sent in checkpoint state so it fires once per session, not every turn)",
        "There is no token-budget trimming of ctx['memories'] before assembly — _enforce_context_budget was removed; memories render as scored/returned by the retriever.",
        "task_body is hard-truncated to _TASK_BODY_CHAR_CAP (3000 chars) at render time, appending an \"...[truncated]\" marker when cut — it has no upstream length cap (a task's body can be arbitrarily long, e.g. a large epic scaffold).",
        "mid_task_decisions (Task decisions section) is capped to the most recent _MAX_DECISIONS (15) entries by langchain_learning/nodes/decision_task.py, sliced on every tasks__add_decision append — not trimmed in dispatcher.py itself. This checkpoint state exists solely for prompt injection; the durable decision history lives in the task's DB record, unaffected by the slice.",
        "execution_contract is rendered as its own section, independent of task_body — it is NOT subject to _TASK_BODY_CHAR_CAP truncation (a fixed north-star template built once at task activation by activate_task.py._build_execution_contract, must stay byte-identical every turn). Cleared alongside the other active-task fields at all three deactivation sites: DeactivateTaskNode (tasks__clear_active/tasks__finish), ActivateTaskNode's pop-to-empty-stack, and LogTaskEventsNode's \"task:<id> done\" auto-close (task:eb46ac47 — this third site was originally missed).",
        "After _format_system_prompt, the UPS handler appends one-shot node output: if ctx['pending_hook_output'].hookSpecificOutput has hookEventName == 'UserPromptSubmit', its additionalContext is appended to the assembled system prompt (e.g. LogTaskEventsNode's /task-introspection nudge after a \"task:<id> done\" auto-close, task:eb46ac47). run_session() surfaces-and-clears the checkpoint copy (mirroring run_post_tool/run_stop), so the payload renders exactly once and cannot leak into the next PostToolUse response; the hookEventName gate prevents a stale cross-event payload from ever being rendered here.",
        "The '## Repo memories' section and repo_task_memories (task:850ddd65's per-repo memory split) were removed by task:a7ddc132 — task_memories now always comes from the global MEMORY.sqlite scorer, with no per-repo alternative source.",
        "CURRENT (task:c2e36050): '## Active task' renders task_id/title from ctx['active_task'] (a dict, or {} if unset) — a live PUSH from task-framework's tasks__set_active/clear_active via hooks/session_state.get_active_task(cwd), populated fresh in _handle_user_prompt_submit every UPS call (task:996cc8f0). Not a pull from any task store, and not the old activate_task.py mechanism this concept otherwise describes."
      ],
      "last_validated": "2026-07-29T18:34:33.582070+00:00",
      "module": "hooks/dispatcher.py",
      "name": "dispatcher-system-prompt-assembly"
    },
    "gates-commit-traceability": {
      "confidence": 0.96,
      "contracts": [
        "Returns (True, reason) for commits missing task:<id> in both the command string and any -F/--file target",
        "Returns (False, '') for all non-commit Bash calls, including calls that merely contain the words git and commit in unrelated segments",
        "Returns (False, '') when task:<id> is found in the command string OR inside a file passed via -F/--file",
        "A missing or unreadable -F file is treated as no match (deny), not a crash",
        "Detection catches `git commit`, `git -C <path> commit`, `git --no-pager commit`, commits behind wrappers (sudo/env/time/nohup) or leading VAR=value assignments, commits after a leading `cd ... &&`, and git_local.sh"
      ],
      "created_at": "2026-06-29T09:47:25.285858+00:00",
      "description": "GitCommitGate enforces that every version-control commit (via Bash or the MCP tool) includes a task:<id> reference for traceability. Commit DETECTION is per shell segment and command-word based (_is_git_commit / _segment_is_git_commit, task:e247268e): the command is split on newline / ; / && / || / | / parens, and a segment counts only if its leading token is `git` (with global flags skipped before reading the subcommand) or a path whose basename is git_local.sh. The task:<id> SEARCH still runs against the whole command string, not per segment, because heredoc messages put the id on a different line from the invocation. The reference may instead live in a file named via -F/--file (git commit -F <path>) — the form task-framework's own /commit skill recommends for multi-paragraph messages, since heredocs and -m chains mangle them.",
      "evidence": [
        "hooks/gates.py:_is_git_commit",
        "hooks/gates.py:GitCommitGate",
        "hooks/gates.py:_FILE_FLAG_RE",
        "hooks/server_memory.py:record_tool_from_hook",
        "tests/test_gates.py"
      ],
      "invariants": [
        "Every commit must reference task:<id> somewhere the gate can see — the command string or a -F/--file target, not necessarily the command string alone",
        "Non-commit Bash calls are never blocked by this gate. This is now true by construction rather than by intent: detection asks whether a shell segment's COMMAND WORD is git/git_local.sh, so the words appearing inside an echo, a string literal, or a shell comment cannot trigger it. Before task:e247268e the regex bridged shell separators and newlines, and denied things like `git status` followed by `echo \"nothing to commit\"`.",
        "_segment_is_git_commit never raises: shlex.split throws ValueError on the unbalanced quotes that heredocs and $(...) fragments routinely produce, so it falls back to a plain split. A gate that denies Bash must degrade rather than except, or one malformed command breaks every Bash call.",
        "hooks/server_memory.py's record_tool_from_hook shares this detector by importing _is_git_commit — it is the second consumer, and it inherited the same false positives until task:e247268e. Changing detection changes what lands on the session timeline as a 'git commit' event, not just what the gate blocks.",
        "git_local.sh is matched on the segment's command word (basename), never as a substring anywhere in the command — otherwise `echo \"git_local.sh\"` reintroduces the false-positive class.",
        "Gate logic runs inside a long-running uvicorn hooks.server:app process (port 8766) — editing hooks/gates.py has no effect until that process restarts; client.py is only a thin HTTP wrapper per call"
      ],
      "last_validated": "2026-08-01T18:10:34.358832+00:00",
      "module": "hooks/gates.py",
      "name": "gates-commit-traceability"
    },
    "gates-prereq-chain-enforcement": {
      "confidence": 0.97,
      "contracts": [
        "check(tool, ctx) returns (deny: bool, reason: str)",
        "deny=True means the tool must be blocked; reason is shown to Claude"
      ],
      "created_at": "2026-06-29T09:47:25.285339+00:00",
      "description": "Anti-hallucination send-gate system. Verifies tool calls against session history before allowing them. Each gate is a composable chain of verifier factories loaded from gate_rules.yaml or hardcoded subclasses.",
      "evidence": [
        "hooks/gates.py"
      ],
      "invariants": [
        "Gates operate only on tool call records written by the hook infrastructure, never on model memory",
        "External gate rules from gate_rules.yaml never override internal Python gates",
        "All gates fail open on DB errors — a broken gate never blocks tools"
      ],
      "last_validated": "2026-06-29T09:47:25.285339+00:00",
      "module": "hooks/gates.py",
      "name": "gates-prereq-chain-enforcement"
    },
    "hook-logs-sqlite-autoprune-at-50k": {
      "confidence": 0.9,
      "contracts": [
        "Callers reading hook_logs (e.g. hooks__read_logs_sqlite MCP tool) should treat it as a rolling ~40-50k row window of recent activity, not a complete historical log -- older entries are permanently gone once pruned, not archived."
      ],
      "created_at": "2026-07-21T22:35:38.512586+00:00",
      "description": "SQLiteHandler.emit() writes every log record to hook_logs in claude_hooks.sqlite (iCloud-synced), then checks COUNT(*) on every single insert. Once the table reaches 50,000 rows, it prunes down to the newest 40,000 rows in one DELETE, keyed by ts DESC (not id DESC).",
      "evidence": [
        "src/logger.py:SQLiteHandler.emit"
      ],
      "invariants": [
        "hook_logs is capped at 50,000 rows -- it auto-prunes to 40,000 on the insert that crosses the threshold, so the table oscillates between ~40k and 50k rows over time, never growing unbounded.",
        "Pruning keeps the newest rows by ts (timestamp), not by id/rowid -- id values become non-contiguous after a prune, so id (or max(id)) is not a valid proxy for total row count or table age.",
        "The COUNT(*) check runs on every emit() call (every log line), not on a schedule/batch -- this is an O(n) check per write, acceptable at this volume but a scaling consideration if log volume grows sharply.",
        "Pruning is silent -- emit() swallows all exceptions (bare except: pass), so a failed prune or count-check never surfaces as an error."
      ],
      "last_validated": "2026-07-25T18:53:11.617148+00:00",
      "module": "src/logger.py",
      "name": "hook-logs-sqlite-autoprune-at-50k"
    },
    "hooks-client-cwd-forwarding": {
      "confidence": 0.9,
      "contracts": [
        "Usage: python3 client.py <HookEvent>",
        "Reads hook payload JSON from stdin, POSTs to {CLAUDE_HOOKS_SERVER or http://127.0.0.1:8766}/hook/{EVENT}, prints the response JSON to stdout",
        "Prints 'claude-hooks: client.py invoked for {EVENT}' to stderr on every invocation, regardless of success/failure"
      ],
      "created_at": "2026-07-08T11:29:24.786447+00:00",
      "description": "Thin HTTP client for the FastAPI hook server. Reads the hook JSON payload from stdin (which already carries the real per-session cwd from Claude Code) and POSTs it to localhost:8766/hook/{event}.",
      "evidence": [
        "hooks/client.py"
      ],
      "invariants": [
        "CLAUDE_CWD env var is a fallback only, used when payload['cwd'] from stdin is missing/empty — it must never unconditionally overwrite a cwd Claude Code already supplied, since CLAUDE_CWD reflects whatever cwd the long-running server process happened to launch from, not the calling session's cwd (bug found 2026-07-08: unconditional overwrite clobbered every session's reported cwd with a stale/empty env var)",
        "Fails open: if the server is unreachable, exits 0 with an empty JSON object rather than blocking the hook event",
        "Every invocation prints a one-line 'claude-hooks: client.py invoked for {EVENT}' announcement to stderr immediately after EVENT is resolved, unconditionally (before the try/except) — this exists because server-side dispatcher logs only show the server's view, giving no signal that client.py itself ran for a given hook event (task:fb7d3777)"
      ],
      "last_validated": "2026-07-25T20:50:36.296661+00:00",
      "module": "hooks/client.py",
      "name": "hooks-client-cwd-forwarding"
    },
    "hooks-mcp-tools-observability": {
      "confidence": 0.95,
      "contracts": [
        "handle_server_memory(n_events) returns a markdown table or {error: ...}",
        "handle_read_logs_sqlite(level, logger, search, limit, format=\"toon\") returns a TOON-encoded string by default (\"count: N\\nrows[N]{...}:\\n  ...\"), or {count, rows} dict when format=\"json\"",
        "handle_session_id() returns {session_id, turn} or {error: ...} after one retry on empty first response",
        "handle_checkpoint_query(thread_id=\"\") returns {thread_id, turn_count, prompt_id, keywords, memories, tool_hints, session_context, ...} or {error: ...}; thread_id doubles as session_id, omitted thread_id resolves to the current session via GET /session/current — no 'domains' key as of task:5b15dc9b (2026-08-07), since nothing in SessionState computes domains anymore"
      ],
      "created_at": "2026-06-29T09:47:25.288414+00:00",
      "description": "MCP tools for querying hook state: server_memory (live chronological timeline via HTTP), checkpoint_query (thin HTTP wrapper over the live server's GET /session/{session_id}, reading in-process MemorySaver state — no longer a SQLite reader as of task:da29c842), and read_logs_sqlite (filtered log reader from claude_hooks.sqlite). Also session_id (added 2026-07-05, task:61703ef7): returns current session_id via GET /session/current with a built-in one-shot retry, absorbing the checkpoint-write race for a brand-new session.",
      "evidence": [
        "src/tools/hooks.py",
        "src/tools/hooks.py:handle_session_id",
        "src/tools/hooks.py:handle_read_logs_sqlite",
        "src/tools/hooks.py:handle_checkpoint_query",
        "src/toon.py:rows_to_toon",
        "tests/test_checkpoint_query_integration.py"
      ],
      "invariants": [
        "server_memory hits the live FastAPI server at 127.0.0.1:8766 — fails if server is down",
        "read_logs_sqlite reads directly from iCloud DB path",
        "session_id retries once (0.5s sleep) if the first /session/current response has no session_id — absorbs the race where a brand-new session's first checkpoint write lags behind the query",
        "checkpoint_query is a thin HTTP wrapper over GET /session/{session_id} — it does NOT read ~/.claude/langgraph_checkpoints.db directly (that sqlite path was retired in production by task:b3964f85; the prior invariant here claiming checkpoint_query reads that file as 'the live DB SqliteSaver writes to in production' was true when confirmed 2026-07-05 (task:61703ef7) but went stale after task:b3964f85's later migration — corrected 2026-07-27, task:da29c842, after the stale assumption caused an unhandled 'no such table: checkpoints' error). checkpoint_query has no 'latest checkpoint across all threads' mode anymore — omitted thread_id resolves only to the current session, matching hooks__session_id's semantics.",
        "read_logs_sqlite's `format` param defaults to \"toon\" (task:d2165715): rows share a uniform schema (id, ts, level, logger, message), which TOON's tabular header+rows encoding compresses well versus repeating JSON keys per row. format=\"json\" is kept as an explicit fallback returning the original {count, rows} dict shape — callers needing the dict shape (e.g. programmatic filtering) must pass format=\"json\" explicitly.",
        "The TOON encoder itself (rows_to_toon/toon_cell) was extracted to src/toon.py (task:4b5bf21f) so src/tools/tasks.py's handle_list could reuse it — hooks.py now imports rows_to_toon rather than defining its own copy.",
        "A docstring/comment's 'confirmed on <date>' claim is only as fresh as the last re-check against the code it describes — checkpoint_query's docstring cited a specific confirmation date (task:61703ef7) that read as settled fact but silently went stale after a later migration nobody circled back to update. Re-verify dated confirmations against current runtime behavior, don't trust the date alone."
      ],
      "last_validated": "2026-08-07T14:51:02.862085+00:00",
      "module": "src/tools/hooks.py",
      "name": "hooks-mcp-tools-observability"
    },
    "live-session-audit": {
      "confidence": 0.9,
      "contracts": [
        "live_claude_sessions() matches ps comm basename == 'claude', excluding MCP subprocess paths",
        "NoopNode only logs when len(sessions) > 1 — silent in the normal single-session case",
        "GET /session/live and NoopNode's passive log call the same live_claude_sessions() helper"
      ],
      "created_at": "2026-08-11T15:03:48.660566+00:00",
      "description": "NoopNode (langchain_learning/nodes/noop.py) logs a warning on every Stop event when more than one `claude` CLI process is live, listing each pid's elapsed runtime — surfaces forgotten detached tmux/terminal sessions passively. GET /session/live on the FastAPI hook server exposes the same ps-based check on demand. Both share one helper, live_claude_sessions(), so they can't drift apart. Replaces the removed session-brief-on-/clear feature (session-brief-segment-cursor, session-brief-worker-detached-summarizer) as this repo's answer to 'is a stray session running.'",
      "evidence": [
        "langchain_learning/nodes/noop.py:live_claude_sessions",
        "hooks/server.py"
      ],
      "invariants": [],
      "last_validated": "2026-08-11T15:03:48.660566+00:00",
      "module": "",
      "name": "live-session-audit"
    },
    "log-tool-usage-single-writer": {
      "confidence": 0.92,
      "contracts": [
        "__call__(state) -> {prompt_tools, session_tools} state update"
      ],
      "created_at": "2026-08-01T05:48:37.369390+00:00",
      "description": "Upserts a tool_hints.sqlite row per tool call and seeds keywords in the same connection. Used to also run _upsert_task_event_tools concurrently via a ThreadPoolExecutor, writing to task_events on EVERY tool call — removed (task:87ec7876) because task_events no longer exists. Found by grep for _cfg.tasks_db rather than the open_tasks/_connect patterns task:6240c675's sweep used, which is why it survived that earlier pass. With one writer left, the pool bought nothing; __call__ now calls _upsert_tool_hint directly.",
      "evidence": [
        "langchain_learning/nodes/log_tool_usage.py:LogToolUsageNode",
        "langchain_learning/nodes/log_tool_usage.py:LogToolUsageNode.__call__"
      ],
      "invariants": [
        "Writes to tool_hints.sqlite only — no task store dependency",
        "Test sessions (api-test-/test-/pytest- prefixes) skip the upsert entirely"
      ],
      "last_validated": "2026-08-01T05:48:37.369390+00:00",
      "module": "langchain_learning/nodes/log_tool_usage.py",
      "name": "log-tool-usage-single-writer"
    },
    "logger-sqlite-handler": {
      "confidence": 0.97,
      "contracts": [
        "setup(name) returns a logger with SQLiteHandler for hook scripts",
        "get_logger(name) returns a 'lc.*' prefixed logger for the LCEL pipeline"
      ],
      "created_at": "2026-06-29T09:47:25.287736+00:00",
      "description": "Shared SQLiteHandler that writes all hook and LangChain pipeline logs to claude_hooks.sqlite in iCloud. Singleton per DB path. Redirects to test DB when CLAUDE_HOOKS_TEST_LOG_DB env var is set. Auto-trims at 50k rows.",
      "evidence": [
        "src/logger.py:SQLiteHandler",
        "src/logger.py:setup"
      ],
      "invariants": [
        "SQLiteHandler is a singleton per db_path — never creates duplicate handlers",
        "emit() never raises — handleError() is a no-op",
        "Log table is auto-trimmed to 40k rows when it exceeds 50k",
        "Test redirect affects only the default singleton, not custom-path handlers"
      ],
      "last_validated": "2026-06-29T09:47:25.287736+00:00",
      "module": "src/logger.py",
      "name": "logger-sqlite-handler"
    },
    "mcp-dispatcher-domain-tool-map": {
      "confidence": 0.98,
      "contracts": [
        "build_dispatcher(mcp) registers all tools from DOMAIN_MAP onto the MCPServer instance"
      ],
      "created_at": "2026-06-29T09:47:25.286803+00:00",
      "description": "MCPServer tool registry. Maps domain names to (module, [action]) pairs. Each action becomes an MCP tool named domain__action. All tool modules live under src/tools/. The 'tasks' entry was removed (task:87ec7876) along with tools/tasks.py — build_dispatcher imports every DOMAIN_MAP module unconditionally at startup, so a stale entry here would have broken server boot, not just one tool call. (mcp 2.x renamed FastMCP to MCPServer, imported from mcp.server; the .tool() registration API is unchanged — task:9376b389.)",
      "evidence": [
        "src/dispatcher.py:DOMAIN_MAP"
      ],
      "invariants": [
        "Tool names follow domain__action convention",
        "New tools require adding an entry to DOMAIN_MAP — no auto-discovery",
        "Handler functions must be named handle_<action> in their module",
        "Every DOMAIN_MAP entry's module must be importable — build_dispatcher loads all of them unconditionally at startup"
      ],
      "last_validated": "2026-08-28T07:50:37.787195+00:00",
      "module": "src/dispatcher.py",
      "name": "mcp-dispatcher-domain-tool-map",
      "related": []
    },
    "mcp-server-bootstrap": {
      "confidence": 0.95,
      "contracts": [
        "mcp.run() starts the MCPServer when invoked as __main__"
      ],
      "created_at": "2026-06-29T09:47:25.288066+00:00",
      "description": "MCP server entry point. Instantiates MCPServer(\"claude-hooks\"), calls build_dispatcher to register all tools, then bootstraps by ensuring Ollama is running and seeding tool_hints keywords. (mcp 2.x renamed FastMCP to MCPServer, imported from mcp.server — task:9376b389. The task TurboVec index rebuild referenced in older versions of this concept was removed in task:87ec7876/task:d6ddb40f.)",
      "evidence": [
        "mcp_server.py:_ensure_ollama",
        "mcp_server.py:_bootstrap"
      ],
      "invariants": [
        "Ollama must be running for semantic task neighbor search to work",
        "Bootstrap failures are logged and swallowed — never crash the server"
      ],
      "last_validated": "2026-08-28T07:50:42.374490+00:00",
      "module": "mcp_server.py",
      "name": "mcp-server-bootstrap",
      "related": []
    },
    "memory-scoring-global-batch-limit": {
      "confidence": 0.9,
      "contracts": [
        "score_memories() fetches at most batch_limit candidate rows total, across the whole memories table, before applying combination-signal scoring."
      ],
      "created_at": "2026-07-21T22:21:23.339835+00:00",
      "description": "score_memories() fetches candidate memory rows via a single aggregate query across the whole memories table (SELECT name, type, tags, body, related, updated FROM memories ORDER BY updated DESC LIMIT batch_limit), then applies combination-signal scoring. Rewritten from a per-domain batched design (task:4c9c21e6, 2026-08-07) after MEMORY.sqlite's domain column was dropped — domain is no longer a retrieval axis, so there is no per-domain SELECT to keep separate.",
      "evidence": [
        "langchain_learning/nodes/_memory_scoring.py:score_memories"
      ],
      "invariants": [
        "batch_limit (memory_scoring.json / src.config.memory_batch_limit) is now a single global cap across all memories, not a per-domain cap.",
        "Truncation (when the table exceeds batch_limit rows) always drops the stalest rows first, via ORDER BY updated DESC -- never an arbitrary rowid-order slice.",
        "The prior per-domain starvation protection (a small/new domain could never be crowded out by a large one) has NO replacement as of this rewrite -- a large volume of recently-updated memories can now crowd out older ones regardless of topic. Accepted as a known, undefended risk when domain was removed (task:4c9c21e6 grooming), not an oversight; revisit only if starvation is observed in practice."
      ],
      "last_validated": "2026-08-07T13:05:00.000000+00:00",
      "module": "langchain_learning/nodes/_memory_scoring.py",
      "name": "memory-scoring-global-batch-limit"
    },
    "rag-core-turbovec-index": {
      "confidence": 0.97,
      "contracts": [
        "load_index(tvim_path, meta_path) returns (IdMapIndex | None, dict)",
        "save_index(index, meta, tvim_path, meta_path) is atomic",
        "query_index(index, meta, q_vec, k) returns list[dict] with id, score, plus meta fields"
      ],
      "created_at": "2026-06-29T09:47:25.288769+00:00",
      "description": "Generic TurboVec RAG core shared by code_rag and diff_rag. Provides load/save/query operations on IdMapIndex (.tvim) + .meta.json sidecar pairs. Atomic saves via tmp-then-rename. Callers supply precomputed query vectors.",
      "evidence": [
        "src/tools/rag_core.py:load_index",
        "src/tools/rag_core.py:save_index"
      ],
      "invariants": [
        "Index and metadata sidecar are always written together atomically",
        "load_index returns (None, {}) on any error — callers must handle missing index",
        "query_index is stateless — no caching, no side effects"
      ],
      "last_validated": "2026-06-29T09:47:25.288769+00:00",
      "module": "src/tools/rag_core.py",
      "name": "rag-core-turbovec-index"
    },
    "server-fastapi-hook-server": {
      "confidence": 0.95,
      "contracts": [
        "POST /hook/{event} calls dispatcher handler and returns hookSpecificOutput, a top-level decision/reason dict (Stop only), or {}",
        "GET /health returns {status: ok}",
        "GET /session/memory returns last N events from server_memory",
        "GET /session/current returns {session_id, turn} from the most recent checkpoint — no active task required. GET /session/active was removed (task:8529435a); task-framework owns active-task state, queried via tasks__active",
        "POST /set-active-taskid accepts {workspace, task_id, title}, stores it in an in-memory dict keyed by workspace; empty/missing task_id clears the entry. GET /session/active-task?workspace=... reads it back, or {} if nothing was ever pushed for that workspace (task:996cc8f0)",
        "_safe_json(request) -> dict — always returns a dict, never raises; malformed/empty/missing body returns {} with a logged warning"
      ],
      "created_at": "2026-06-29T09:47:25.286219+00:00",
      "description": "Persistent FastAPI server replacing per-invocation subprocess dispatcher. Routes POST /hook/{event} to dispatcher handlers. Uses MemorySaver (in-process, in-memory LangGraph checkpointer) — does NOT survive server restarts. Replaced SqliteSaver (task:b3964f85) after two corruption incidents. Per-thread checkpoint history is capped at 5000 rows, enforced from NoopNode on every Stop event. Single uvicorn worker process, event loop is uvloop (task:53c9f817). All 6 hook endpoints parse the request body via a shared _safe_json() helper (task:33419f95) that fails open to {} on malformed/empty JSON instead of raising — matches hooks/client.py's own fail-open design.",
      "evidence": [
        "hooks/server.py",
        "hooks/server.py:session_current",
        "hooks/server.py:set_active_taskid",
        "pyproject.toml",
        "~/Library/LaunchAgents/com.debaditya.claude-hooks-pipeline.plist",
        "hooks/server.py:_safe_json",
        "tests/test_server_api.py"
      ],
      "invariants": [
        "Checkpoint state lives only in-process memory (MemorySaver) — discarded on every server restart/reload; nothing about session/task-activation state persists across a restart except what server_memory.py separately logs to SQLite",
        "Per-thread checkpoint history is capped at 5000 rows via NoopNode on every Stop event, not by the server itself",
        "Server runs on port 8766 as a single uvicorn worker process — deliberately not multi-worker/per-session",
        "GET /session/current reads live in-memory MemorySaver state (not a DB); it does not require an active task — GET /session/active (which did) was removed (task:8529435a)",
        "POST /set-active-taskid/GET /session/active-task hold a live PUSH cache from task-framework (task:996cc8f0), not a pull-derived echo like the removed GET /session/active — task-framework remains the durable source of truth, this is just the last value it reported, discarded on restart same as everything else here",
        "Event loop is uvloop, not stdlib asyncio — controlled entirely by whether uvloop is installed",
        "All 6 /hook/{event} endpoints (UserPromptSubmit, PreToolUse, PostToolUse, Stop, SessionStart, SessionEnd) parse their body via the shared _safe_json(request) helper, which catches any JSON-parse failure and returns {} instead of letting it propagate to the generic exception handler as a 500 — found missing (task:33419f95, log audit 2026-07-26) after an empty/malformed body crashed pre_tool_use with an unhandled JSONDecodeError. Any NEW hook endpoint added here must use _safe_json too, not a bare `await request.json()`, to preserve this fail-open guarantee."
      ],
      "last_validated": "2026-08-11T15:03:48.659252+00:00",
      "module": "hooks/server.py",
      "name": "server-fastapi-hook-server"
    },
    "server-memory-consolidated-timeline": {
      "confidence": 0.96,
      "contracts": [
        "record_prompt/record_tool/record_task write to both layers",
        "get(n_events) returns from in-memory cache only (no DB hit at read time)"
      ],
      "created_at": "2026-06-29T09:47:25.286494+00:00",
      "description": "Two-layer consolidated context store (SQLite + in-memory cache) answering 'what was I working on?'. Stores prompts, MCP tool calls, and task activations as a chronological event sequence. Write-through: every insert hits both SQLite and the in-memory cache.",
      "evidence": [
        "hooks/server_memory.py"
      ],
      "invariants": [
        "Test sessions (prefixed test-, pytest-, api-test-) are never written to the store",
        "Rolling window capped at 1000 events — oldest deleted on overflow",
        "Cache is hydrated from SQLite at server startup so context survives reloads"
      ],
      "last_validated": "2026-06-29T09:47:25.286494+00:00",
      "module": "hooks/server_memory.py",
      "name": "server-memory-consolidated-timeline"
    },
    "server-memory-tool-recording": {
      "confidence": 0.92,
      "contracts": [
        "record_tool_from_hook(body) and record_task_from_hook(body) take raw PostToolUse dicts",
        "A Bash call is recorded as a 'git commit' event only when _is_git_commit(command) is true; the recorded args are the comma-joined task:<id> references found anywhere in the command, or None",
        "_result_snippet(tresp) extracts and truncates to _RESULT_MAX (100) chars: unwraps the MCP content envelope ({'content':[{'type':'text','text':...}]}) the same way _title_from_response does; falls back to json.dumps(tresp) for non-MCP dict shapes (e.g. Read/Edit/Write), then str(tresp) as a last resort; returns None if the resulting text is empty"
      ],
      "created_at": "2026-06-29T09:47:25.289140+00:00",
      "description": "Hook payload helpers that extract and record tool calls from raw PostToolUse payloads. MCP tools recorded with compact JSON args. Read/Edit/Write recorded with file_path. Bash only recorded for version-control operations — decided by hooks.gates._is_git_commit, imported rather than reimplemented (task:e247268e). tasks__set_active recorded as task type not tool type, from ANY MCP server providing that name — this is an observability layer, not a rule enforcer, so recording activity from other servers' tasks is correct. Title used to be resolved authoritatively from proj_tasks.db (task:87ec7876 removed that lookup, _title_for_task); it now comes only from the tool's own response, which was already the fallback path for any task the DB lookup missed. Since task:27a9c6ed, every recorded case (MCP tools, Read/Edit/Write, git-commit Bash) also carries a truncated result snippet extracted from body['tool_response'] via _result_snippet().",
      "evidence": [
        "hooks/server_memory.py:record_tool_from_hook",
        "hooks/server_memory.py:record_task_from_hook",
        "hooks/server_memory.py:_result_snippet",
        "hooks/gates.py:_is_git_commit"
      ],
      "invariants": [
        "record_task_from_hook never queries a task store — title comes from the tool response only",
        "Commit detection is SHARED with GitCommitGate via hooks.gates._is_git_commit, never a local copy. Until task:e247268e this module imported the gate's old _GIT_COMMIT_RE, and so silently inherited its false positives: any Bash call merely containing the word 'commit' after a git subcommand was recorded as a 'git commit' timeline event. Historical rows written before that fix include phantom commits, so the timeline is not trustworthy as a commit log for that period.",
        "A change to what counts as a commit changes this module's output too, not just what the gate blocks — the two consumers cannot be reasoned about independently.",
        "result snippets are stored as first-100-chars-as-is with no redaction — server_memory.sqlite is a local-only store already trusted with full prompts and args, so this is a deliberate consistency choice (task:27a9c6ed), not an oversight. Revisit only if a concrete leak is observed.",
        "Other Bash calls (non-git-commit) remain entirely unrecorded — 'too noisy' — so there is no result to attach to them by construction"
      ],
      "last_validated": "2026-08-09T02:15:50.014612+00:00",
      "module": "hooks/server_memory.py",
      "name": "server-memory-tool-recording"
    },
    "session-graph-test-production-routing": {
      "confidence": 0.9,
      "contracts": [
        "get_session_graph(session_id: str = '') -> a compiled, checkpointer-backed StateGraph, production or test depending on session_id prefix"
      ],
      "created_at": "2026-07-28T01:58:19.215098+00:00",
      "description": "get_session_graph(session_id) is the single chokepoint every hook entry point (run_session, run_gate, run_post_tool, run_stop, prewarm_session) already calls to obtain the compiled LangGraph StateGraph. Since task:b63088a1, it also decides WHICH of two graph instances to return based on session_id's prefix, isolating integration-test checkpoint traffic from the shared production MemorySaver without changing any endpoint URL or code path.",
      "evidence": [
        "langchain_learning/session_graph.py:get_session_graph",
        "langchain_learning/session_graph.py:TEST_SESSION_PREFIX",
        "hooks/server.py:session_detail",
        "hooks/dispatcher.py:_handle_session_end",
        "tests/test_session_graph_test_isolation.py"
      ],
      "invariants": [
        "Any session_id starting with TEST_SESSION_PREFIX ('integration-test-') routes to _test_graph, a separate lazily-built graph/MemorySaver instance; every other session_id (including '') routes to the production _graph, exactly as before this routing existed.",
        "Both _graph and _test_graph are built lazily on first use via the identical build_session_graph(checkpointer=MemorySaver()) pattern — no special-casing between them except which module-level global gets populated.",
        "A checkpoint written under a test-prefixed session_id is genuinely invisible to the production graph, not just conventionally separated — they are two distinct MemorySaver instances with independent .storage dicts.",
        "No eviction/cleanup logic exists for _test_graph and none is needed: both graphs reset to empty on every server restart, since MemorySaver has no persistence and none is intended for either.",
        "Callers that reference the underlying singleton directly (sg._graph) rather than through get_session_graph(session_id) will NOT get isolation-aware routing — hooks/server.py's session_detail endpoint and hooks/dispatcher.py's _handle_session_end both had to be updated to route through get_session_graph(session_id) (or check the correct global explicitly) for the isolation guarantee to hold end-to-end."
      ],
      "last_validated": "2026-07-28T01:58:19.215098+00:00",
      "module": "langchain_learning/session_graph.py",
      "name": "session-graph-test-production-routing"
    },
    "session-state-latest-checkpoint-lookup": {
      "confidence": 0.9,
      "contracts": [
        "get_current_session() -> {session_id, turn} or {} if no checkpoint exists anywhere"
      ],
      "created_at": "2026-07-28T01:58:12.018834+00:00",
      "description": "get_current_session backs GET /session/current — the only signal available for 'what session am I in' before any thread_id is known. Uses _latest_checkpoint_tuple() to find the single most-recently-written checkpoint across all threads. get_active_session (and GET /session/active) were removed (task:8529435a): active_task_id/active_task_title stopped being written into checkpoint state once task:882d67fa moved active-task ownership fully to task-framework, so the function always returned {}. Ask task-framework (tasks__active) for the live answer instead.",
      "evidence": [
        "hooks/session_state.py:_latest_checkpoint_tuple"
      ],
      "invariants": [
        "No active-task concept lives in this module anymore — session_state.py only ever reports session_id/turn from the checkpoint, never a task id"
      ],
      "last_validated": "2026-08-01T05:48:37.367361+00:00",
      "module": "hooks/session_state.py",
      "name": "session-state-latest-checkpoint-lookup"
    },
    "toon-encoding-shared-module": {
      "confidence": 0.9,
      "contracts": [
        "toon_cell(value) -> str: stringifies value, wraps in double quotes with internal quotes doubled if it contains a comma, newline, or quote character",
        "rows_to_toon(rows: list[dict]) -> str: returns 'rows[0]{}:' for an empty list, otherwise 'rows[N]{field1,field2,...}:' header line followed by one two-space-indented comma-joined line per row"
      ],
      "created_at": "2026-07-26T02:53:41.011542+00:00",
      "description": "Shared TOON (Token-Oriented Object Notation) encoder for MCP tool responses that return arrays of uniform-schema dicts. Originally extracted from src/tools/hooks.py (task:d2165715) so src/tools/tasks.py's handle_list (task:4b5bf21f) could reuse it without duplicating the comma/quote/newline escaping logic. That second consumer no longer exists — src/tools/tasks.py was deleted by task:87ec7876 — so as of 2026-08-01 the only production caller is src/tools/hooks.py's handle_read_logs_sqlite. The module is kept as a standalone unit (rather than folded back into hooks.py) because it is independently unit-tested in tests/test_toon.py and is the natural home for any future TOON-emitting tool.",
      "evidence": [
        "src/toon.py:rows_to_toon",
        "src/tools/hooks.py",
        "tests/test_toon.py"
      ],
      "invariants": [
        "Only worth using for a list of dicts sharing the same keys, returned to an LLM caller rather than a human reading raw output — nested/heterogeneous data (e.g. concept_store/concepts.json entries) loses to plain JSON under TOON, so this is not a blanket replacement for JSON responses repo-wide.",
        "rows_to_toon() uses the FIRST row's keys as the header and does not fill in missing keys for later rows — callers MUST normalize every row to an identical, consistent key set before calling it, or the tabular header will not match later rows. (Historical example, now deleted: handle_list in src/tools/tasks.py did this for the optional _context_only field, defaulting it to False on every row.)",
        "Any existing MCP tool being defaulted to TOON output must have its internal (non-MCP, in-process Python) callers grepped for and updated to pass format=\"json\" explicitly — the unit test suite alone is not sufficient signal, since unit tests typically only exercise the tool module directly, not other in-process callers integrating with it. (Historical example, both modules now deleted: task:4b5bf21f found hooks/ui/routes.py calling tasks.py's handle_list() directly in 5 places expecting list[dict], caught only by the integration suite at deploy time, not the unit gate.)"
      ],
      "last_validated": "2026-08-01T17:52:06.216210+00:00",
      "module": "src/toon.py",
      "name": "toon-encoding-shared-module"
    }
  },
  "meta": {
    "commit": "5f62c6b5150a20fe7198d7bd6c155c4b3c0d3101",
    "extracted_at": "2026-07-23T05:42:23Z",
    "note": "incremental upsert (task:a2fd402d)",
    "updated_at": "2026-08-28T07:50:42.374537+00:00"
  }
}
